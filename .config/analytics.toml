# ==========================================
# General Settings
# ==========================================

"SEED" = 24434

"forecast_horizon" = 90

# ==========================================
# Imputation Configuration
# ==========================================

"impute_cols" = [
    "staffing_level_fte",
    "channel_mix_index",
    "backlog_days",
]

"impute_predictor_cols" = [
    "recovered_complaints",
    "media_mentions",
    "staffing_level_fte",
    "channel_mix_index",
    "backlog_days",
]

"max_iter_iterative_imputer" = 100

"imputer_trial_count" = 50

# ==========================================
# Forecasting Configuration
# ==========================================

"forecast_predictor_cols" = [
    "recovered_complaints",
    "media_mentions",
    "staffing_level_fte",
    "channel_mix_index",
    "backlog_days",
]

"model_names" = [
    "ridge",
    "random_forest",
    "hist_gradient_boosting",
]
"model_trial_count" = 50

# ==========================================
# Visualization Configuration
# ==========================================

"columns_for_viz" = [
    # "complaints",
    "media_mentions",
    # "backlog_days",
    # "staffing_level_fte",
    # "centered_7d_mean",
]

# ==========================================
# MLflow Configuration
# ==========================================

"mlflowlog" = "no" # yes / no

"mlflow_experiment_name" = "phso_complaint_forecast"

"mlflow_tracking_uri" = "mlflowresult"